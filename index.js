let currentUser = "Ricardo Colon Medina";

let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

let excitedWelcomeMessage = welcomeMessage.toUpperCase();

let shortGreeting = `${welcomeMessage.split(" ", 1)[0]}, ${currentUser[0]}!`;

